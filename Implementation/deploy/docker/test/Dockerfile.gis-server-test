FROM idispatchx/gis-server
# Tile fixtures baked into the image (generated by generate-tiles.sh, gitignored).
# Build this image from the SampleData/tiles/ directory as context:
#   docker build -t idispatchx/gis-server-test \
#     -f Implementation/deploy/docker/test/Dockerfile.gis-server-test \
#     SampleData/tiles/
COPY . /tiles/
